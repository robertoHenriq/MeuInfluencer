version: '3.8'
services:
  db:
    image: postgres:15
    container_name: postgres-db
    restart: always
    environment:
      POSTGRES_USER: admin123
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: MeuInfluencer
    ports:
      - "5432:5432"